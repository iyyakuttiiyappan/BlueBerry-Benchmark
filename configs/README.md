# Configs

`fresh_benchmark_514.yaml` is the reproducibility configuration used for the 514-image benchmark experiments.

Before running locally, edit:

- `data_root`
- `paths.fixed_split_manifest`
- hardware settings such as `batch_size`, `num_workers`, and `epochs`

This benchmark bundle intentionally does not include dataset download links. Add the final accepted data citation later.
